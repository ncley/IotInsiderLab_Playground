
    export class BiasesForDevices {
        constructor(public deviceId: string, public shadowName: string, public countBias: number, public angerBias: number, public contemptBias: number, public disgustBias: number, public fearBias: number, public happinessBias: number, public neutralBias: number, public sadnessBias: number, public surpriseBias: number, public device: Devices) {
        }
    }
 